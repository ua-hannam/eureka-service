server:
  port: 8761

spring:
  application:
    name: eureka-service
  cloud:
    config:
      name: config
      uri:
  profiles:
    active: eureka-prod

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans